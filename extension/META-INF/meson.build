# Install bundled manifest
# plain 'manifest.xml' is for .oxt only

if bundled
	install_data(
		'bundled_manifest.xml',
		rename: 'manifest.xml',
		install_dir: join_paths(extension_dir, 'META-INF')
	)
endif